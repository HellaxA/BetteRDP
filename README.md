Screen capturing options:
1) xcap
https://crates.io/crates/xcap
- has dependencies for linux to install: apt-get install pkg-config libclang-dev libxcb1-dev libxrandr-dev libdbus-1-dev libpipewire-0.3-dev libwayland-dev libegl-dev
2) implementing yourself
- very low level and will probably require unsafe rust.

Screen displaying options:
1) minifb
